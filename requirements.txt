python-dotenv>=1.0.0
mariadb>=1.1.0
paramiko>=3.0.0
sshtunnel>=0.4.0
qrcode[pil]>=7.4.0
# Pinned to 52.5 — the last release that works with Pango 1.42 (AlmaLinux 8).
# WeasyPrint 53+ uses Pango 1.44 features (e.g. round_glyph_positions) that
# AlmaLinux 8 doesn't ship. 52.5 still produces clean PDFs for the limited
# CSS this project uses. See:
# https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
# (search for "version 52.5 of WeasyPrint")
weasyprint==52.5
jinja2>=3.1.0
html2text>=2020.1.16
